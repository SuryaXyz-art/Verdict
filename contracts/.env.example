# Deployer wallet (testnet only — never commit the real .env)
PRIVATE_KEY=

# Somnia testnet RPC (default works; override if using a provider)
SOMNIA_RPC_URL=https://api.infra.testnet.somnia.network

# Somnia Agents platform (AgentRequester) — testnet
AGENT_REQUESTER=0x037Bb9C718F3f7fe5eCBDB0b600D607b52706776

# LLM Inference agent ID — copy from https://agents.testnet.somnia.network
LLM_AGENT_ID=
